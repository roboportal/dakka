version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build:integration -w @roboportal/integration_page"
  artifacts:
    baseDirectory: /projects/integration/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
