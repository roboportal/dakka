name: Integration

on:
  pull_request:
    branches: [ master ]
  push:
    branches: [ master ]

jobs:
  integration_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - shell: bash
        run: |
          npm ci
      - shell: bash
        run: |
          npm ci --prefix ./test/integration
      - shell: bash
        run: |
          npm run install --prefix ./test/integration
      - shell: bash
        run: |
          npm run build --prefix ./test/integration
      - shell: bash
        run: |
          npm run generate --prefix ./test/integration
      - shell: bash
        run: |
          npm run test --prefix ./test/integration
    