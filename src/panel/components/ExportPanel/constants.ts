export enum exportOptions {
  none = 'none',
  cypress = 'cypress',
  playwright = 'playwright',
  puppeteer = 'puppeteer',
  dakka = 'dakka',
}

export const ativeTags = ['button', 'a', 'input', 'textarea', 'select']
